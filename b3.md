1. Phân tích các điểm vi phạm Clean Code của đoạn mã ban đầu
1. Tên biến không có ý nghĩa
public double calc(List<Account> list, String branch, boolean activeOnly)
calc quá mơ hồ.
list không thể hiện đây là danh sách tài khoản.
a không mô tả đối tượng gì.

Nên đổi thành:

calculateBranchBalance(...)
accountList
account
2. If-else lồng nhau quá sâu

Code hiện tại có tới 6 cấp điều kiện:

if (list != null)
    if (a != null)
        if (a.getBranch().equals(branch))
            if (activeOnly)
                if (a.getStatus().equals("ACTIVE"))
                    if (a.getBalance() > 0)

Vi phạm nguyên tắc:

Arrow Code
Deep Nesting
Khó đọc
Khó bảo trì
3. Magic String
"ACTIVE"

Hard-code trực tiếp trong code.

Nên thay bằng:

private static final String ACTIVE_STATUS = "ACTIVE";

hoặc Enum.

4. Null Safety kém
a.getBranch().equals(branch)

Có thể gây:

NullPointerException

nếu branch của account bằng null.

5. Không tận dụng Java 17

Đang dùng vòng lặp thủ công:

for (...)

Trong khi Java 17 hỗ trợ Stream API.

6. Không Logging

Khi tính toán:

Không biết đã duyệt bao nhiêu tài khoản.
Không biết tổng tiền bao nhiêu.
Khó debug Production.
7. Chưa tối ưu cho Spring Boot

Nếu đây là service đọc dữ liệu:

@Transactional(readOnly = true)

nên được bổ sung.

2. Prompt Vòng 1 - Robustness & Clean Code

Bạn là Senior Java Developer và Clean Code Reviewer.

Hãy refactor đoạn mã Java dưới đây theo các nguyên tắc Clean Code.

Yêu cầu:

Loại bỏ toàn bộ if-else lồng nhau sâu.
Áp dụng Guard Clauses hoặc Return Early.
Đổi tên:
calc -> calculateBranchBalance
list -> accountList
a -> account
Tăng khả năng đọc hiểu.
Không thay đổi nghiệp vụ.
Giữ nguyên Java 17.

Đoạn mã cần refactor:

import java.util.List;

public class LedgerBalanceCalculator {

    public double calc(List<Account> list, String branch, boolean activeOnly) {

        double total = 0;

        if (list != null) {

            for (Account a : list) {

                if (a != null) {

                    if (a.getBranch().equals(branch)) {

                        if (activeOnly) {

                            if (a.getStatus().equals("ACTIVE")) {

                                if (a.getBalance() > 0) {

                                    total += a.getBalance();

                                }

                            }

                        } else {

                            if (a.getBalance() > 0) {

                                total += a.getBalance();

                            }

                        }

                    }

                }

            }

        }

        return total;
    }
}

Trước khi sinh code hãy giải thích ngắn gọn các vấn đề Clean Code tìm thấy.

3. Prompt Vòng 2 - Maintainability & OOP

Tiếp tục từ phiên bản đã refactor ở vòng trước.

Nhiệm vụ:

Chuyển hoàn toàn sang Java 17 Stream API.
Loại bỏ vòng for truyền thống.
Tăng tính Maintainability.
Code ngắn gọn nhưng dễ đọc.
Nếu class hoạt động trong Spring Boot:
thêm @Service
thêm @Transactional(readOnly = true)
Không thay đổi nghiệp vụ.
Giữ nguyên kết quả tính toán.

Trước khi xuất code:

Phân tích lợi ích của Stream API.
Mô tả luồng xử lý mới.
4. Prompt Vòng 3 - Logging & Context Tuning

Tiếp tục từ phiên bản ở vòng 2.

Nhiệm vụ:

Tích hợp Lombok @Slf4j.
Ghi log.info:
số lượng account được truyền vào
tổng số dư tính được
branch đang xử lý
Logging phải theo chuẩn Production.
Giữ nguyên Stream API.
Giữ nguyên @Transactional(readOnly = true).
Java 17.
Spring Boot.

Trước khi sinh code:

Giải thích chiến lược logging.
Chỉ rõ các thông tin cần ghi log.
Sau đó xuất toàn bộ class hoàn chỉnh cuối cùng.
5. Minh chứng phản hồi AI ở vòng cuối
Phân tích chiến lược Logging
Log số lượng account đầu vào để hỗ trợ giám sát dữ liệu.
Log branch đang xử lý để phục vụ truy vết.
Log tổng số dư cuối cùng để hỗ trợ kiểm tra nghiệp vụ.
Sử dụng log.info vì đây là thông tin nghiệp vụ thông thường.
LedgerBalanceCalculator hoàn chỉnh
package com.securebank.ledger;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;
import java.util.Objects;

@Slf4j
@Service
@Transactional(readOnly = true)
public class LedgerBalanceCalculator {

    private static final String ACTIVE_STATUS = "ACTIVE";

    public double calculateBranchBalance(
            List<Account> accountList,
            String branch,
            boolean activeOnly) {

        if (accountList == null || accountList.isEmpty()) {

            log.info(
                    "No accounts found for branch={}",
                    branch
            );

            return 0D;
        }

        double totalBalance =
                accountList.stream()
                        .filter(Objects::nonNull)
                        .filter(account ->
                                branch.equals(account.getBranch()))
                        .filter(account ->
                                account.getBalance() > 0)
                        .filter(account ->
                                !activeOnly ||
                                ACTIVE_STATUS.equals(
                                        account.getStatus()))
                        .mapToDouble(Account::getBalance)
                        .sum();

        log.info(
                "Ledger calculation completed. branch={}, totalAccounts={}, totalBalance={}",
                branch,
                accountList.size(),
                totalBalance
        );

        return totalBalance;
    }
}
Các cải tiến đạt được
Vòng	Cải tiến
Vòng 1	Loại bỏ deep nesting, đổi tên biến rõ nghĩa
Vòng 2	Chuyển sang Stream API, bổ sung @Transactional(readOnly = true)
Vòng 3	Tích hợp @Slf4j, logging theo chuẩn Production

Kết quả cuối cùng là mã nguồn ngắn gọn, dễ đọc, dễ bảo trì và phù hợp môi trường Spring Boot doanh nghiệp.
