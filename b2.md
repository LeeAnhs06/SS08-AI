1. Nội dung Prompt gỡ lỗi do thiết kế

Bạn là một Senior Java Debugger và Software Architect chuyên xử lý lỗi hệ thống Java + JDBC + Database.

Hãy phân tích sự cố dựa trên mã nguồn và Stack Trace dưới đây.

import java.sql.Connection;
import java.sql.PreparedStatement;

public class TransactionRepository {

    private Connection connection;

    public void saveTransaction(String transactionId, Long userId, double amount) throws Exception {

        // Lỗi logic: Không kiểm tra xem userId có tồn tại trong bảng Users hay không
        // Dẫn đến lỗi vi phạm khóa ngoại (Foreign Key Constraint Violation)

        String sql = "INSERT INTO transactions (id, user_id, amount) VALUES (?, ?, ?)";

        PreparedStatement ps = connection.prepareStatement(sql);

        ps.setString(1, transactionId);
        ps.setLong(2, userId);
        ps.setDouble(3, amount);

        ps.executeUpdate();
    }
}
org.h2.jdbc.JdbcSQLIntegrityConstraintViolationException:
Referential integrity constraint violation:
"CONSTRAINT_FOREIGN_KEY_USER:
PUBLIC.TRANSACTIONS FOREIGN KEY(USER_ID)
REFERENCES PUBLIC.USERS(ID) (99)";

SQL statement:

INSERT INTO transactions
(id, user_id, amount)
VALUES (?, ?, ?)

[23506-214]

at org.h2.message.DbException.getJdbcSQLException(DbException.java:502)

at TransactionRepository.saveTransaction(TransactionRepository.java:13)

at Main.main(Main.java:10)

Hãy đóng vai Java Debugger chuyên nghiệp và thực hiện:

Bước 1:
Giải nghĩa Stack Trace từng dòng.

Bước 2:
Xác định chính xác Root Cause của lỗi.

Bước 3:
Giải thích vì sao lỗi xảy ra trong cơ sở dữ liệu.

Bước 4:
Đề xuất hướng sửa theo chuẩn Enterprise Application.

Bước 5:
Refactor toàn bộ TransactionRepository.

Trước khi INSERT:

Kiểm tra userId có tồn tại trong bảng users hay không.
Nếu không tồn tại:
Không thực hiện INSERT.
Không để Database ném Foreign Key Exception.
Ném UserNotFoundException.

Tạo thêm:

UserNotFoundException
Hàm checkUserExists(Long userId)
TransactionRepository phiên bản an toàn

Yêu cầu:

Java 17
JDBC thuần
Clean Code
Try-with-resources
Đóng tài nguyên đúng cách
Có comment giải thích các đoạn quan trọng
Phân tích lỗi.
Root Cause.
Thuật toán xử lý mới.
Mã nguồn UserNotFoundException.
Mã nguồn TransactionRepository đã sửa hoàn chỉnh.
2. Minh chứng chạy thực tế (mô phỏng phản hồi AI)
Phân tích Stack Trace
org.h2.jdbc.JdbcSQLIntegrityConstraintViolationException

→ H2 Database phát hiện vi phạm ràng buộc toàn vẹn dữ liệu.

Referential integrity constraint violation

→ Vi phạm khóa ngoại (Foreign Key Constraint).

FOREIGN KEY(USER_ID)
REFERENCES PUBLIC.USERS(ID)

→ Cột transactions.user_id đang tham chiếu tới users.id.

(99)

→ Hệ thống đang cố chèn giao dịch với user_id = 99.

at TransactionRepository.saveTransaction(TransactionRepository.java:13)

→ Lỗi phát sinh tại dòng thực hiện executeUpdate().

Root Cause

Nguyên nhân gốc rễ:

Bảng transactions có khóa ngoại:
FOREIGN KEY (user_id)
REFERENCES users(id)
Giá trị user_id = 99 không tồn tại trong bảng users.

Ví dụ:

users

id
--
1
2
3

Nhưng hệ thống thực hiện:

INSERT INTO transactions
(id, user_id, amount)
VALUES ('TX001', 99, 1000);

Do user 99 không tồn tại nên Database từ chối INSERT và ném ngoại lệ.

Thuật toán xử lý mới
1. Nhận transactionId, userId, amount

2. Kiểm tra userId có tồn tại trong bảng users không

3. Nếu không tồn tại:
      throw UserNotFoundException

4. Nếu tồn tại:
      thực hiện INSERT

5. Đóng PreparedStatement đúng cách

6. Trả kết quả thành công
UserNotFoundException
package com.securebank.exception;

public class UserNotFoundException extends Exception {

    public UserNotFoundException(Long userId) {
        super("User not found with id: " + userId);
    }
}
TransactionRepository phiên bản an toàn
package com.securebank.repository;

import com.securebank.exception.UserNotFoundException;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;

public class TransactionRepository {

    private final Connection connection;

    public TransactionRepository(Connection connection) {
        this.connection = connection;
    }

    /**
     * Lưu giao dịch an toàn.
     */
    public void saveTransaction(
            String transactionId,
            Long userId,
            double amount)
            throws SQLException, UserNotFoundException {

        if (!checkUserExists(userId)) {
            throw new UserNotFoundException(userId);
        }

        String sql =
                "INSERT INTO transactions " +
                "(id, user_id, amount) " +
                "VALUES (?, ?, ?)";

        try (PreparedStatement ps =
                     connection.prepareStatement(sql)) {

            ps.setString(1, transactionId);
            ps.setLong(2, userId);
            ps.setDouble(3, amount);

            ps.executeUpdate();
        }
    }

    /**
     * Kiểm tra user tồn tại hay không.
     */
    private boolean checkUserExists(Long userId)
            throws SQLException {

        String sql =
                "SELECT 1 FROM users WHERE id = ?";

        try (PreparedStatement ps =
                     connection.prepareStatement(sql)) {

            ps.setLong(1, userId);

            try (ResultSet rs = ps.executeQuery()) {
                return rs.next();
            }
        }
    }
}
Kết quả sau khi sửa
Không còn phụ thuộc vào việc Database ném Foreign Key Exception.
Lỗi được phát hiện sớm ở tầng Repository.
Trả về ngoại lệ nghiệp vụ rõ ràng UserNotFoundException.
Dễ debug, dễ bảo trì và phù hợp chuẩn Enterprise Application.
